set $mod Mod4
set $alt Mod1


title_align left

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:JetBrainsMono 8


# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
# exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
# set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% # && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10%  #&& $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle  #&& $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle  #&& $refresh_i3status
bindsym XF86TouchpadToggle exec --no-startup-id touchpad-ctl --toggle

# Monitor Brightness
# bindsym XF86MonBrightnessDown exec --no-startup-id brightness-ctl -10
# bindsym XF86MonBrightnessUp exec --no-startup-id brightness-ctl +10
bindsym XF86MonBrightnessUp exec --no-startup-id polybar-msg action "#backlight.inc"
bindsym XF86MonBrightnessDown exec --no-startup-id polybar-msg action "#backlight.dec"
bindsym --release $mod+Shift+o exec --no-startup-id /home/obyoxar/.obydots/bin/toggle-opaque

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod


# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
# kill focused window
bindsym $mod+Shift+q kill
# start dmenu (a program launcher)
bindsym --release $mod+space exec rofi -show drun
# start ranger (a program launcher)
bindsym --release $mod+numbersign exec kitty ranger
bindsym --release $mod+plus exec kitty kalker
# start haskell ghci
bindsym --release $mod+c exec kitty ghci
# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+p exec --no-startup-id zen-toggle

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+i split h
# focus the parent container
bindsym $mod+u focus parent
# split in vertical orientation
bindsym $mod+o split v

# bindsym --release $mod+v exec alacritty --hold -e xprop 

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+f floating toggle
bindsym $mod+s move scratchpad
bindsym $mod+Shift+m scratchpad show


# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle


# focus the child container
#bindsym $mod+d focus child


focus_on_window_activation focus

# workspace_auto_back_and_forth yes
# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10:discord"

# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace $ws10
# bindsym $mod+Tab workspace back_and_forth

# move workspace to monitor
bindsym $mod+$alt+l move workspace to output right
bindsym $mod+$alt+h move workspace to output left
bindsym $mod+$alt+1 exec --no-startup-id i3-ws-to-monitor 1
bindsym $mod+$alt+2 exec --no-startup-id i3-ws-to-monitor 2
bindsym $mod+$alt+3 exec --no-startup-id i3-ws-to-monitor 3
bindsym $mod+$alt+4 exec --no-startup-id i3-ws-to-monitor 4
bindsym $mod+$alt+5 exec --no-startup-id i3-ws-to-monitor 5
bindsym $mod+$alt+6 exec --no-startup-id i3-ws-to-monitor 6

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+x exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+e exec "rofi -show power-menu -modi power-menu:rofi-power-menu -lines 6 -width 20"
bindsym $mod+z exec "rofi -show zoxide -modi zoxide:rofi-zoxide"
bindsym $mod+Shift+s exec "rofi -show settings -modi settings:rofi-settings"
bindsym $mod+Tab exec "rofi -show window"

# Open Dunst Context Menu
bindsym $mod+period exec "dunstctl context"
 

# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt
        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

exec_always --no-startup-id $HOME/.config/polybar/launch.sh &
exec --no-startup-id picom -b
exec --no-startup-id dunst
# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet
exec --no-startup-id pasystray


# Bar
# bar {
        # status_command i3status
	# i3bar_command $HOME/.config/polybar/launch.sh
# }



# Looks
default_border pixel

gaps inner 15
gaps outer 0

mode "gaps" {

	bindsym $mod+i gaps inner current plus 5
	bindsym $mod+Shift+i gaps inner current minus 5

	bindsym $mod+b gaps inner current toggle 60
	bindsym $mod+r gaps inner current toggle 15

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+g mode "default"
}
bindsym $mod+g mode "gaps"

set $background_color #9D59D2
# set $orange_1 #fe8019
# set $orange #d65d0e

# exec_always --no-startup-id hsetroot -solid "$background_color"
# exec_always --no-startup-id hsetroot -cover "$HOME/obydots/ternary-design/background.png"
exec_always --no-startup-id zen-toggle --set false

# set $w_bord_col #fcf431
# set $w_indicator #9d59d2ff
set $w_indicator #fcf431
set $w_bord_col #9d59d2ff

# [[[ Copied but modified from https://github.com/dracula/i3/blob/master/.config/i3/config
# class                 border  bground text    indicator child_border
client.focused          #6272A4 #6272A4 #F8F8F2 #bd93f9   #6272A4
client.focused_inactive #44475A #44475A #F8F8F2 #44475A   #44475A
client.unfocused        #282A36 #282A36 #BFBFBF #282A36   #282A36
client.urgent           #44475A #FF5555 #F8F8F2 #FF5555   #FF5555
client.placeholder      #282A36 #282A36 #F8F8F2 #282A36   #282A36

client.background       #F8F8F2
# ]]]


# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork -i ~/obydots/ternary-design/background_nb.png -k --inside-color=9d59d2 --ring-color=f9f5f0 --insidever-color=9d59d2 --ringver-color=7d3ab1 --insidewrong-color=9d59d2 --ringwrong-color=ed7e7a --line-color=7d3ab1 --keyhl-color=7d3ab1 --bshl-color=ddd60e --greeter-color=ffffff --layout-color=f9f5f0 --time-color=f9f5f0 --date-color=f9f5f0 --verif-color=f9f5f0 --wrong-color=f9f5f0 --modif-color=f9f5f0 --verif-text="" --wrong-text="" --time-font="JetBrainsMono NF" --date-font="JetBrainsMono NF" --timeoutline-width=20
exec --no-startup-id xss-lock --transfer-sleep-lock -- obydots-lock
exec --no-startup-id udiskie -t


# Default Window rules
# Telegram as a small "applet-like" window
for_window [class="TelegramDesktop"] floating enable
for_window [class="TelegramDesktop"] resize set width 100 px
for_window [class="TelegramDesktop"] resize set height 100 px
for_window [class="black-pixels-voxel-demo"] floating enable
# For some reason after an update visual studio insists on spawning with a title bar, which is annoying... Lets force it to comply
for_window [class="Code"] border pixel

# Window specific stuff
assign [class="discord"] workspace number $ws10
#Temp stuff for bummydame
assign [class=".*[^t].py$"] workspace number $ws10
assign [class="voxelrust"] workspace number $ws5

for_window [class="further-down"] fullscreen enable
assign [class="further-down"] workspace number $ws1
