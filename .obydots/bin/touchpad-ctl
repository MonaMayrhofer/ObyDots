#!/usr/bin/env bash

source $(obydots-profile-file touchpad-interface.sh)

while [[ $# -gt 0 ]]; do
	case $1 in
		"-t"|"--toggle")
			touchpad-toggle
			;;
		*)
			echo "Unrecognized parameter: $1"
	esac
	shift
done


