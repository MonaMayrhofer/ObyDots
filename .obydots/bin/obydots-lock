#!/usr/bin/env bash

while [[ $# -gt 0 ]]; do
	case $1 in
	"--help"|"-h") echo "Help not yet written :) Hihihi"
		exit 1
		;;
	*)
		echo "Unknown argument $1"
		exit 1
		;;
	esac
	shift
done

source $(obydots-profile-file session-locker.sh)

start-locker

exit 0
